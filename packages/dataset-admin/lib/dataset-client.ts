import { createDatasetClient } from '@supeffective/dataset'

export const datasetClient = createDatasetClient({
  baseDataUrl: 'https://itsjavi.com/supereffective-dataset/data',
  baseImageUrl: 'https://itsjavi.com/supereffective-assets/assets',
})
