{
  "name": "@supeffective/dataset",
  "version": "0.0.1",
  "description": "Dataset for supereffective.gg",
  "homepage": "https://github.com/itsjavi/supereffective-dataset#readme",
  "bugs": {
    "url": "https://github.com/itsjavi/supereffective-dataset/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/itsjavi/supereffective-dataset.git"
  },
  "license": "MIT",
  "type": "module",
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "pnpm build:data && pnpm data:minify && pnpm build:js",
    "build:data": "bun ./src/scripts/import.ts && pnpm data:join",
    "build:js": "tsup",
    "data:join": "bun ./src/scripts/data-join.ts",
    "data:minify": "bun ./src/scripts/minify.ts ./data ./dist/data",
    "data:split-v1": "bun ./src/scripts/data-split.ts",
    "dev": "tsup --watch --clean=false",
    "typelint": "tsc --noEmit",
    "validate-data": "bun ./src/validate/validate.ts"
  },
  "dependencies": {
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "@pkmn/dex": "^0.7.52",
    "@pkmn/dex-types": "^0.7.52",
    "@types/node": "^20.7.1",
    "glob": "^10.3.10",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2",
    "zod": "^3.22.2"
  }
}
